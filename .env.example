# Copy this file to .env and fill in your PostgreSQL credentials.
# The app will fall back to in-memory sample data if DATABASE_URL is not set.

DATABASE_URL=postgresql://user:password@localhost:5432/washgo
